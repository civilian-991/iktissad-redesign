# GCC newsroom — offline prompt optimization (DSPy + GEPA)
# Run this OFFLINE (not in the request path); it emits candidate prompt versions
# for human review (never hot-swaps). See README.md.
dspy-ai>=2.5
supabase>=2.6
python-dotenv>=1.0
